#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int a, b;
    scanf("%d%d", &a, &b);
    printf("%d\n", abs(a) + abs(b));
    printf("%d\n", abs(a) - abs(b));
    return 0;
}